## 📝 Post Service

**Post Service** — микросервис управления постами (публикациями) в составе микросервисной платформы.

Сервис отвечает за создание, хранение, обновление и обработку постов, а также за взаимодействие с другими микросервисами через gRPC и событийную модель. Для асинхронного обмена событиями используется Kafka.  
Post Service изолирован от внешнего доступа и работает исключительно внутри системы.

---

### 🔧 Зона ответственности

- создание и управление постами;
- хранение данных постов в PostgreSQL;
- предоставление gRPC API для внутренних сервисов;
- публикация и обработка событий через Kafka;
- управление схемой базы данных с помощью миграций;
- обеспечение целостности и согласованности данных.

---

### 🚀 Технологический стек

- **Python 3.12+**
- **gRPC** — межсервисное взаимодействие
- **PostgreSQL**
- **SQLAlchemy 2.x (async)** — ORM для работы с базой данных
- **asyncpg** — асинхронный драйвер PostgreSQL
- **Alembic** — миграции базы данных
- **Kafka (aiokafka)** — событийное взаимодействие между сервисами
- **Pydantic Settings** — управление конфигурацией
- **python-dotenv** — загрузка переменных окружения
- **Poetry** — управление зависимостями и сборкой
- **Docker** — контейнеризация и деплой

---

### 🧩 Взаимодействие в системе

- принимает gRPC-запросы от:
  - API Gateway;
  - других внутренних микросервисов;
- публикует и/или потребляет события через Kafka;
- использует собственную изолированную базу данных;
- не имеет прямого HTTP-доступа извне.

---

### 📦 Структура проекта (пример)

